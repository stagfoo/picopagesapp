gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
# Exception: app/debug.keystore is intentionally committed (debug-only,
# never used for release signing) so CI and local builds share a stable
# debug signing identity — see app/build.gradle.kts signingConfigs.debug.
!app/debug.keystore
